% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_check_duplicates.R
\name{fn_check_duplicates}
\alias{fn_check_duplicates}
\title{Compare values of duplicate rows.}
\usage{
fn_check_duplicates(
  df,
  id_cols,
  exclude_cols = c(),
  later_cols = c(),
  return_df = "new"
)
}
\arguments{
\item{df}{Data frame to check.}

\item{id_cols}{Character vector of columns used to identify which rows to compare.}

\item{exclude_cols}{Optional. Character vector of columns left out entirely.}

\item{later_cols}{Optional. Character vector of columns only compared if rows are otherwise identical.}

\item{return_df}{Optional. Whether to return the \code{"input"} or a \code{"new"} data frame. Defaults to \code{return_df = "new"}}
}
\value{
Input or new data frame with columns is_empty (boolean), status (string), matched_rows (string), and one or more \if{html}{\out{<col>}}_match (boolean).
}
\description{
Description goes here.
}
\examples{
fn_check_duplicates(LEMURSpkg::LEMURS_dupe_df,
  id_cols      = c("surveyID", "recordID"),
  exclude_cols = c("progress", "variablR"),
  later_cols   = c("variablC"))
}
