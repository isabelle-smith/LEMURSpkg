% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fn_read_qualtrics_data.R
\name{fn_read_qualtrics_data}
\alias{fn_read_qualtrics_data}
\title{Read-in and lightly clean a Qualtrics CSV.}
\usage{
fn_read_qualtrics_data(
  full_file_path,
  col_types_list = list(.default = "c"),
  unique_id,
  drop_cols = c(),
  num_vars = c(),
  int_vars = c(),
  key_df = NULL
)
}
\arguments{
\item{full_file_path}{File path to CSV file from Qualtrics. Assumes headers and 2 rows of Qualtrics info are present.}

\item{col_types_list}{Optional. List of column types for \code{\link[readr:read_delim]{readr::read_csv()}}.}

\item{unique_id}{One of "PID", "record_id", or "uvmid+uvmSurveyID". Column(s) specified must be present in file.}

\item{drop_cols}{Optional. Character vector of columns to remove. Not required to be present in file.}

\item{num_vars}{Optional. Character vector of columns to convert to numeric via \code{\link[=as.numeric]{as.numeric()}}. Not required to be present in file.}

\item{int_vars}{Optional. Character vector of columns to convert to integer via \code{\link[=as.integer]{as.integer()}}. Not required to be present in file.}

\item{key_df}{Data frame with columns uvmid and record_id; used only if \code{unique_id="uvmid+uvmSurveyID"}.}
}
\value{
Data frame without \code{drop_cols}, other Qualtrics columns, and rows that have NA in \code{unique_id}.
}
\description{
Description goes here.
}
\details{
Qualtrics columns that are removed: Status, IPAddress, RecipientLastName, RecipientFirstName, RecipientEmail, ExternalReference, LocationLatitude, LocationLongitude, DistributionChannel, UserLanguage.

Qualtrics columns that are kept: StartDate, EndDate, Progress, Duration (in seconds), Finished, RecordedDate, ResponseId, ResponseID, SurveyID, and any user-created others.
}
\examples{
## ADD EXAMPLES
## put file(s) in `extdata` directory
}
